server:
  port: ${SERVER_PORT:8080}
  servlet:
    session:
      timeout: ${SESSION_TIMEOUT:2m} # 2m

spring:
  application:
    name: spring-security

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring-security
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
  jpa:
    show-sql: ${SHOW_SQL:true} # default: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: ${FORMAT_SQL:true} # default: false

# Custom Configuration for in-memory user store
#  security:
#    user:
#      name: user
#      password: password01
#      roles:
#        - USER


logging:
  level:
    org.springframework.security: ${SPRING_SECURITY_LOG_LEVEL:TRACE} # default: Error